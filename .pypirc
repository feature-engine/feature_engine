[distutils]
  index-servers =
    pypi
    PROJECT_NAME

[pypi]
  username = ${{ secrets.PYPI_USERNAME }}
  password = ${{ secrets.PYPI_PASSWORD }}

[PROJECT_NAME]
  repository = https://upload.pypi.org/legacy/
  username = ${{ secrets.PYPI_USERNAME }}
  password = ${{ secrets.PYPI_PASSWORD }}