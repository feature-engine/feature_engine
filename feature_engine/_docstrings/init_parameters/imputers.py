_missing_only_docstring = """missing_only: bool, default=False
        If `missing_only` equals `True` and `variables` equals `None` then the transformer 
        will select and impute the variables that show missing values during `fit()`. 
        Consequently, the transformer will only learn from the variables that are comprised of 
        missing values.
"""
